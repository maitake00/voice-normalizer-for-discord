# Discord Voice Normalizer — プロジェクト仕様

受信側で、Discord通話の話者ごとの音量を自動的に一定レベルへ揃えるツール。
OSSとして公開する前提で設計する。

---

## 0. 前提と制約（設計判断の根拠）

- Discordには話者ごとの音量を自動で揃える機能が存在しない。ユーザー個別の音量スライダー（0〜200%）を手動で調整するしかない。
- Discordデスクトップアプリの出力は**ミックス済みの1本**。OSレベルでは話者ごとに音声を分離できない。
- したがって本ツールは**音声を加工しない**。測定してDiscord自身の「ユーザーの音量」設定を書き換える**フィードバック制御**として実装する。
  - 利点: 遅延ゼロ、音質劣化ゼロ、Discord側に設定が永続化される。

---

## 1. アーキテクチャ

```
[capture]   Discordプロセスの出力音声を取得（OS依存）
[speaking]  誰が喋っているか（Discord RPC のイベント）
[core]      ラウドネス推定 + 制御ループ ← プラットフォーム非依存の純粋ロジック
[sink]      volume% を書き込む（RPC / 将来的にクライアントプラグイン）
```

**最重要**: `core` はOS処理・IOから完全に分離し、純粋関数群として実装する。
合成音声を入力して収束をユニットテストできる状態を保つこと。ここがOSSとしての品質を決める。

### モジュール構成（想定）

```
src/
  core/
    loudness.py      # K-weighting フィルタ、ブロックラウドネス算出
    estimator.py     # ユーザーごとのローリングヒストグラム + パーセンタイル推定
    controller.py    # 目標との差分 → volume% を決定する制御ループ
  capture/
    windows.py       # WASAPI プロセスループバック（pyaudiowpatch）
    # linux.py       # PipeWire（v0.3）
  discord/
    rpc.py           # SPEAKING_START/STOP 購読、SET_USER_VOICE_SETTINGS
  app.py             # 常駐ループ、設定読み込み、状態の永続化
tests/
  test_core.py       # 合成音声での収束テスト（IO一切なし）
```

---

## 2. コアアルゴリズム

推定器は**1つだけ**にする。複数のゲート手法を積み重ねないこと（後述の「やらないこと」参照）。

```
1. speakingフラグでゲート
   - Discord RPC の SPEAKING_START/STOP を購読
   - 「今ちょうど1人だけ喋っている」区間のフレームのみ採用
   - 2人以上重複している区間は破棄（実通話ではほとんどが単独発話なのでサンプルは足りる）

2. K-weighting を適用
   - BS.1770 の K-weighting フィルタのみ借用（知覚に近く、軽い）
   - ※ BS.1770 に「準拠」しようとしないこと。理由は §5 参照

3. 100ms ブロックごとに dB を算出し、ユーザーごとのローリングヒストグラムに蓄積

4. 素の声量を逆算（★ ここが肝）
     L_raw = L_measured - 20*log10(volume / 100)
   測定値は自分がかけたゲイン込みなので、これをしないとフィードバックが発散する

5. 高めのパーセンタイル（初期値は要実測）を「その人の声量」とする
   - 平均ではなくパーセンタイル。混入したノイズフレームは下側に落ちて無視される

6. 最低サンプル数に達するまで補正しない（寡黙な人で推定が暴れるのを防ぐ）

7. 制御ループ
   - 目標値との差分から新しい volume%（0〜200 の整数）を算出
   - デッドバンド ±1.5dB（微差では動かさない）
   - 1回の補正は ±3dB まで、EMA でゆっくり収束
   - 200% に張り付くユーザーは救えない → 「マイクゲインを上げてもらってください」と通知する
```

### パラメータ（これ以上増やさない）

| 名前 | 説明 | 初期値 |
|---|---|---|
| `target_db` | 目標ラウドネス | 要実測 |
| `percentile` | 声量とみなすパーセンタイル | 要実測（85〜90あたりから） |
| `window` | ヒストグラムの窓長 | 要実測 |
| `min_samples` | 補正を開始する最低サンプル数 | 要実測 |
| `deadband_db` | 補正しない差分の幅 | 1.5 |
| `max_step_db` | 1回の補正上限 | 3.0 |

**設定可能にするのは上記のみ。説明できないパラメータを増やさないこと自体が品質。**

---

## 3. パーセンタイル値のトレードオフ（ドキュメントに書くこと）

これは技術的最適解が存在しない、**ポリシーの選択**である。

- 高め（90%点）→ 大声のピークが揃う。耳は守られるが、ボソボソ喋る人は相変わらず聞こえない
- 低め（中央値）→ 小声の人が聞こえる。代わりにその人が叫んだ瞬間に爆音

デフォルトは決め打ちにせず、設定可能にした上でREADMEでこの二律背反を説明する。

---

## 4. 既知の罠（実装前に必ず対処）

1. **アテニュエーション**
   Discordの「誰かが喋ると他の音を下げる」機能がONだと**測定値が汚染される**。
   RPCで現在値を読み、0%でなければ起動時に警告する。これを見落とすと「なぜか収束しない」で数日溶ける。

2. **ゲーム音の混入**
   出力デバイス丸ごとではなく、**Discordプロセスだけ**をループバック録音する。

3. **Go Live（配信）の音声**
   同じ出力に混ざり測定を汚す。除外ロジックが必要。

4. **同時発話による破棄率**
   破棄しすぎると収束しない。破棄率をログで可視化しておく。

5. **200%の壁**
   Discordの上限。ソフト側では救えないので、ユーザーに通知する設計にする。

---

## 5. やらないこと（明示的な非目標）

- **BS.1770への準拠**: 絶対ゲート(-70 LUFS)はマイクノイズ(-50dBFS前後)を一切除去せず無意味。相対ゲート(-10 LU)はパーセンタイルの粗い版で、両方積むと二重ゲートになりデバッグ不能になる。また相対ゲートは全体平均を出してから切り直す二段階処理で本質的にオフライン。K-weightingだけ借りる。
- **測定値の数値的厳密さの追求**: Opus圧縮・Krispのノイズ抑制・自分のvolume%を通過した後の音を測っている以上、LUFSの厳密さはこの経路では意味を持たない。
- **単独発話判別へのAI利用**: Discord RPC が事実として教えてくれる。推定に置き換えるのは設計の後退。
- **VADの初期投入**: 人の声かどうかの判別にはVAD（Silero VAD等）が有効な場面はあるが、まずVADなしで実装し、**測定でどれだけ改善するか実測してから**依存を追加するか決める。そのために測定フレームとラベルをログに落とせるようにしておく。将来的には `vad: none | webrtc | silero` のオプションとし、デフォルトoff。

---

## 6. 配布上の最大の障害：RPCの承認

**未承認アプリはRPCを使えない**（テスターリスト50枠のみ）。よって「配布バイナリに開発者のClient IDを埋め込む」方式は成立しない。

**採用する回避策**: ユーザー自身にDiscord Applicationを作ってもらい、そのClient ID/Secretを設定に入れさせる。
アプリのオーナーは承認なしでRPCを使えるため、これで全員が動く。
セットアップの手間は**初回起動ウィザード**（「Developer Portalを開く → New Application → IDを貼る」）で吸収する。

将来の代替バックエンド: Vencord/BetterDiscordプラグイン版（RPC不要だが、クライアント改造はToSグレー）。
**sink層を抽象化して両方差し替えられる形にしておくこと。**

---

## 7. MVPの切り方

- **v0.1** — Windows + RPC + CLIのみ。単独発話区間だけ測定し、収束過程をログ出力。
  まず自分の通話仲間で回して**実際に収束するかを検証**する。パラメータの初期値はここで決める。
- **v0.2** — タスクトレイ常駐、設定ファイル、200%張り付き警告、初回セットアップウィザード
- **v0.3** — Linux(PipeWire)、Vencordバックエンド

---

## 8. OSSとして必須のこと

他人の音声を扱うツールなので、**信頼の獲得が導入率を決める**。

READMEに必ず書く:
- **音声はメモリ上で解析するだけで、保存も外部送信も一切しない**（該当コードへのリンク付きで示す）
- Discord公式機能ではなく自己責任であること
- プラグインバックエンド使用時の追加リスク
- パーセンタイル設定のトレードオフ（§3）

その他:
- ライセンスは MIT（迷う要素がなければこれで十分）
- **before/after の波形または実際の通話音声を並べた30秒のデモ動画をREADME冒頭に置く**。これの有無で反応が変わる
- 導線: Discord公式フォーラムの音量正規化要望スレ、Vencordのプラグインリクエスト、GitHub topics

---

## 9. 最初のタスク

1. `core/` を IOなしで実装（loudness → estimator → controller）
2. 合成音声（既知のdB差を持つ複数話者を模したもの）で**収束テストを書く**
3. そこが通ってから capture / rpc に着手する
